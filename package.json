{
  "name": "presence-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "PRESENCE — Anti-deepfake physical presence verification system",
  "workspaces": [
    "web",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev:web & npm run dev:server",
    "dev:web": "npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=shared && npm run build --workspace=web && npm run build --workspace=server"
  },
  "keywords": ["deepfake", "verification", "presence", "ultrasonic", "challenge-response"],
  "license": "MIT"
}
