// Bank Transaction Call Simulator
// This page will render the bank call interface that initiates
// the PRESENCE verification flow and broadcasts ultrasonic challenge tokens.

export default function CallPage() {
  return (
    <div>
      {/* TODO: Bank transaction call UI */}
      <h1>Bank Call Simulator</h1>
      <p>PRESENCE verification will be initiated from here.</p>
    </div>
  );
}
